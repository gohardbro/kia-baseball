<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="file">

	<insert id="insertOne" parameterType="file">
		INSERT INTO FILE_DATA
		VALUES
		(FILE_NO_SEQ.NEXTVAL,#{board_no},#{notice_no}, #{file_name},
		#{file_type},
		#{file_size}, #{file_location} )
	</insert>
	
	<select id="selectOne" resultType="file" parameterType="int">
		SELECT
		* FROM FILE_DATA WHERE
		FILE_NO = #{no}
	</select>
	
	<delete id="deleteFile" parameterType="file">
		DELETE FROM FILE_DATA WHERE
		FILE_NO = #{no} AND BOARD_NO = #{b_no}
	</delete>
	
	<delete id="deleteFileList" parameterType="int">
		DELETE FROM FILE_DATA
		WHERE
		FILE_NO = #{no}
	</delete>
</mapper> 